<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:sec="http://www.springframework.org/schema/security"
	xmlns:security="http://www.springframework.org/security/tags"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-4.0.xsd">

	<beans:bean id="hashClass" class="fr.mgs.toolbox.Hasher" />
	<beans:bean id="authManager" class="fr.mgs.services.business.UserService" />


	<http auto-config="true" use-expressions="true">

		<!-- Unique session -->
		<session-management>
			<concurrency-control max-sessions="1"
				expired-url="/homePage" />
		</session-management>

		<intercept-url pattern="/connection" access="permitAll" />
		<intercept-url pattern="/**" access="permitAll" />

		<form-login login-page="/login" 
			login-processing-url="/login_check"
			default-target-url="/productCustomer"
			authentication-failure-url="/login.xhtml?error" 
			username-parameter="username"
			password-parameter="password" />
		<logout logout-success-url="/login?logout" />
		
		<!-- enable csrf protection -->
		<csrf />
	</http>

	<authentication-manager>
		<authentication-provider user-service-ref="authManager">
			<password-encoder ref="hashClass" />
		</authentication-provider>
	</authentication-manager>

</beans:beans>
